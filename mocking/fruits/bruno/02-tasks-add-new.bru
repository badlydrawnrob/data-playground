meta {
  name: Fruits (add new)
  type: http
  seq: 3
}

post {
  url: http://localhost:8000/fruits
  body: json
  auth: inherit
}

body:json {
  {
    "color": 2,
    "image": null,
    "name": "Lime"
  }
}

docs {
  # Fruits (add new)
  
  > `ID` and `URL` are automatically generated
  
  ## Color
  
  `Color` expects an `ID`, but the alternative option is to have a `String` (such as `"red"`) and then have your route function `.get()` that color's `ID`.
}
