meta {
  name: Fruits (update put)
  type: http
  seq: 4
}

put {
  url: http://localhost:8000/fruits/:id
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "color": 2,
    "image": null,
    "name": "Pear"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Fruits (update one)
  
  > Currently a bug that `UUID` in the `ModelIn` should be supplied, but this is a column that shouldn't be touched.
  
  Our `UUID` is automatically applied on `POST`.
}
